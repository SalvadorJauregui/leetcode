class Solution {
    public int majorityElement(int[] nums) {
        int elem = 0, count = 0;
        for (int i : nums){
            if (count == 0){
                elem = i;
                count = 1;
            }else if (i == elem){
                ++count;
            }else{
                --count;
            }
        }
        return elem;
    }
}

class Solution {
    public int singleNumber(int[] nums) {
        int x; = nums[0];
        for (int i = 1; i < nums.length; i++)
            /* XOR operation holds the single number */
            x ^= nums[i];
        return x;
    }
}
